# actionlint configuration
# https://github.com/rhysd/actionlint/blob/main/docs/config.md

paths:
  # release.yml is auto-generated by cargo-dist -- suppress all lint errors
  .github/workflows/release.yml:
    ignore:
      - ".+"
